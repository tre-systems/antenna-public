// How long each slide is on screen before the next one fades in.
export const DWELL_MS = 8000;

// Hide the chrome + controls after this many ms of pointer/keyboard inactivity.
export const IDLE_MS = 3000;
